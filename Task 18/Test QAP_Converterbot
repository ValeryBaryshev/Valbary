Тестирование QAP_Coverterbot

Исправлены следующие ошибки:

1.В файле config.py:
    В переменной keys исправлнен ticker ЮэСДэ на USD
	
2. В файле extensions.py
   Строка (from config import *)	заменена на (from config import keys)
   
После исправления ошибок бот запускается,
 выполняются следующие команды:

/start выдает стартовое сообщение и пишет, какие команды еще он умеет выполнять;
/help напоминает, какие команды умеет выполнять бот, а также как именно их нужно вводить;
/values выписывает все валюты, которые знает бот;
<валюта 1> <валюта 2> <количество> выводит значение количества валюты 2 от валюты 1. 

 При вводе неверных значений валют выводится ошибка пользователя
 При вводе лишних значений выводится ошибка пользователя
